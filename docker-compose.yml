services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: kalabsha
      POSTGRES_PASSWORD: kalabsha
      POSTGRES_DB: kalabsha
    ports: ["5432:5432"]
    volumes: [kalabsha_pgdata:/var/lib/postgresql/data]
volumes:
  kalabsha_pgdata:
