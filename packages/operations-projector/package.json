{
  "name": "@kva/operations-projector",
  "version": "1.0.0",
  "private": true,
  "description": "Operations read-model projector for KVA flight events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "test": "tsx --test test/operations-projector.test.ts"
  },
  "dependencies": {
    "@kva/event-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
