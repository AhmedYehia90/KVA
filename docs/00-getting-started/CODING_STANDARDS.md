# Coding Standards

- TypeScript strict mode
- Avoid unnecessary `any`
- Server actions remain separate from presentation
- Domain logic does not live in UI components
- Events are versioned and consumers are idempotent
- Financial records use immutable ledgers
- Secrets never enter source control
- Every public contract is documented
